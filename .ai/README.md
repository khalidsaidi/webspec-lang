# .ai (Agent Workspace)
This directory is reserved for AI-agent internal artifacts (plans, logs, scratch files, compiled outputs).
It exists to prevent agent artifacts from polluting the application code.

Tracked:
- `.ai/README.md`
- `.ai/.gitkeep`

Not tracked:
- Everything else under `.ai/`
